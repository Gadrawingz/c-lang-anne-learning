#include <stdio.h>

void main() {

    // Operator:
    // is a symbol that perform operations on operands
    
    // 1. Variable declaration(initialization)
    int a;
    int b;
    
    // 2. Variable assignment
    // To assign or add values to variables 
    a = 5;
    b = 3;

    // addition we use + operator
    
    printf("%d \n", a);


}